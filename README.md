Созданны связанные списки. Добавлены методы вставки узлов в:

void push_back(int data); // конец

void push_front(int data); // начало

void insert(int pos, int data); // и середину списка.

void show(); // метод просмотра узлов на экране

void deleteNode(int data); // методы удаления узлов по значению ключа

void deleteNodePosition(int position); // методы удаления узлов по позиции

void clear(); // удаление всего списка.

bool find(int data); // поиск узла по значению


Добавленны классы Стек и Очередь. Добавлены:

	bool push(int x); // операция добавления элемента на вершину стека
 
	int pop(); // операция удаления элемента с вершины стека
 
	int peek(); // операция просмотра элемента с вершины стека без его удаления
 
	bool isFull(); // утилитарная функция, которая показывает заполнен ли стек
 
	bool isEmpty(); // операция проверки существования элементов в стеке
 
void enQueue(int x); //добавить элемент в конец очереди.

void deQueue(); // удалить элемент из начала очереди.

int peek(); // получить значение передней части очереди(первого в очереди), не удаляя ее.

bool isEmpty(); //  проверка, пуста ли очередь.

bool BracketsBalanced(string expr); // возвращает true, если скобки в строке сбалансированы, и false — если нет. Под балансом скобок мы имеем в виду, что все скобки (открывающие и закрывающие) находятся в правильном порядке, и каждая открывающая скобка, имеет свою закрывающую.

Например, для строки: "[()]{}{[()()]()}" функция должна вернуть true, а для строки "[(])" — false:
